# 端口配置规则

## 一、本地开发环境

| 服务 | 端口 | 说明 |
|------|------|------|
| 后端 API | 3001 | 本地开发默认连接测试环境数据库和飞书 |
| 用户端前端 (client) | 5173 | Vite 开发服务器 |
| 管理端前端 (admin) | 5174 | Vite 开发服务器 |

### 本地开发代理配置

本地前端项目通过 Vite proxy 代理到测试环境：

| 项目 | 本地端口 | 代理目标 | 说明 |
|------|----------|----------|------|
| client | 5173 | 118.145.239.169:3002 | 代理到测试环境 nginx |
| admin | 5174 | 118.145.239.169:3002 | 代理到测试环境 nginx |

**⚠️ 重要：本地前端代理到 3002 端口（nginx），不是 3003！**

## 二、远程服务器 - 生产环境

| 端口 | 服务 | 用途 | 外部访问 |
|------|------|------|----------|
| 80 | nginx | 主入口，反向代理到前端静态资源和后端 API | ✅ 开放 |
| 8080 | nginx | 备用访问入口 | ✅ 开放 |
| 3001 | Node.js (PM2: boatman-health-A4) | 后端 API 服务 | ❌ 仅内部 |

**访问地址：**
- 用户端：http://boatman-health.ziya.site
- 管理端：http://boatman-health.ziya.site/admin/
- 备用：http://118.145.239.169:8080

## 三、远程服务器 - 测试环境

| 端口 | 服务 | 用途 | 外部访问 |
|------|------|------|----------|
| 3002 | nginx | 测试前端入口 + API 代理 | ✅ 开放 |
| 3003 | Node.js (PM2: server-test) | 测试后端 API | ❌ 仅内部 |

**访问地址：**
- 测试前端：http://118.145.239.169:3002
- 测试后端 API：通过 nginx (3002) 代理访问，不可直接访问 3003

**⚠️ 重要：3003 端口不对外开放，必须通过 3002 端口的 nginx 代理访问！**

## 四、网络架构

```
本地开发：
┌─────────────┐      代理       ┌─────────────────┐
│ localhost   │ ──────────────▶ │ 测试环境 nginx  │
│ :5173/:5174 │                 │ :3002           │
└─────────────┘                 └────────┬────────┘
                                         │ 内部转发
                                         ▼
                                ┌─────────────────┐
                                │ 测试后端 Node   │
                                │ :3003 (内部)    │
                                └─────────────────┘

测试环境：
用户浏览器 ──▶ 118.145.239.169:3002 (nginx) ──▶ localhost:3003 (Node API)

生产环境：
用户浏览器 ──▶ boatman-health.ziya.site:80 (nginx) ──▶ localhost:3001 (Node API)
```

## 五、端口分配原则

1. **生产环境端口**：80、8080、3001 - 未经明确授权不得修改
2. **测试环境端口**：3002（nginx，对外开放）、3003（Node，仅内部）
3. **本地开发端口**：3001（后端）、5173（client）、5174（admin）

## 六、重要提醒

- 本地开发默认连接测试环境，不连接生产环境
- 本地前端代理目标是 **3002**（nginx），不是 3003
- 3003 端口不对外开放，只能通过 nginx 代理访问
- 端口冲突时优先检查是否有其他 Node 进程占用
- 生产环境修改必须经过用户明确授权
