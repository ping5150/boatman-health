# 系统迁移工作量说明（给领导版）

## 一句话概括

**这相当于把整套系统从零开始重新搭建一遍**，包括服务器、数据库搬家、飞书三方系统重建、前后端网页部署，以及全部功能点的联调测试。

> 目前生产环境已有 **11 条业务数据**（6 个用户 + 5 条各类表单记录），这些历史数据需要完整迁移到新环境。

---

## 具体要做的事情

### 第一步：搭建新服务器环境（约 0.5 天）

| 内容 | 说明 |
|------|------|
| 装系统基础软件 | Node.js、Nginx、PM2 等 |
| 配置网络 | 开放端口、域名解析、SSL 证书 |
| 建目录结构 | 按规范建好存放代码和数据的文件夹 |

> **需要您提供**：新服务器的登录账号密码、新域名

---

### 第二步：部署后端服务 + 数据库迁移（约 1-1.5 天）

后端是系统的"大脑"，负责处理所有业务逻辑。同时需要把老数据搬过来：

#### 2.1 后端部署

| 内容 | 数量/说明 |
|------|----------|
| 上传后端代码并编译 | 整套程序在新服务器上编译运行 |
| 安装运行依赖 | 约 30+ 个第三方工具包 |
| **新建 5 张数据表** | 用户表、预约表、健康档案表、睡眠问卷表、营养问卷表 |
| 其中营养问卷表特别复杂 | 单张表 **80+ 个字段** |
| 配置启动进程 | 让程序后台持续运行，崩溃自动重启 |

#### 2.2 历史数据迁移 ⭐ 新增

需要从旧服务器导出数据，导入到新服务器，并逐表校验完整性：

| 表名 | 现有数据量 | 迁移内容 | 校验要点 |
|------|-----------|---------|---------|
| 用户表 | **6 人** | 账号密码、个人信息、角色权限 | 用户能否正常登录、权限是否正确 |
| 预约咨询表 | **1 条** | 预约时间、咨询类型、订单号 | 订单信息是否完整 |
| 健康档案表 | **2 份** | 个人健康信息（含既往病史等） | 长文本字段是否丢失 |
| 睡眠问卷 | **1 份** | ~25 项睡眠评估数据 | 每个选项值是否正确 |
| 营养问卷 | **1 份** | **~80+ 项饮食运动数据**（最复杂） | 大量表单数据最容易出问题 |

> **迁移风险点**：
> - 密码是加密存储的，迁移后必须确保用户能用原密码正常登录
> - 各表之间有关联关系（如预约记录关联到具体用户），不能乱序
> - 迁移完成后需要**逐条人工核对**

---

### 第三步：部署前端页面（约 0.5 天）

前端就是用户看到的界面，有两套独立系统：

| 系统 | 用途 | 页面数 |
|------|------|-------|
| **用户端** | 客户手机端（注册登录、预约、填档案等） | **19 个页面** |
| **管理后台** | 内部管理系统（看用户列表、管预约、查档案等） | **10+ 个页面** |

两套都需要单独打包，放到服务器不同位置，配置访问规则。

---

### 第四步：配置飞书表格对接（约 1-1.5 天）⚠️ 最繁琐

系统会把用户提交的数据自动同步到飞书在线表格。换新飞书账号，相当于**重新搭建整个飞书对接体系**。

#### 4.1 在新飞书账号中创建应用（需您配合）

| 操作 | 说明 |
|------|------|
| 创建飞书自建应用 | 给系统办"通行证" |
| 开通多维表格权限 | 让应用能读写飞书表格 |
| 获取密钥对 | 应用 ID + 应用密钥 |

#### 4.2 创建 5 张飞书表格（每张都要配字段）

**错一个字就会同步失败**：

| 表格名 | 字段数 | 内容概要 |
|--------|--------|---------|
| 用户表 | ~15 列 | 姓名、手机、性别、生日、紧急联系人 |
| 预约咨询表 | ~15 列 | 时间、类型、订单号 |
| 健康档案表 | ~50+ 列 | 基本信息、病史、家族史 |
| 睡眠问卷表 | ~25 列 | 入睡时长、起夜次数、打鼾情况等 |
| **营养问卷表** | **~80+ 列** | 身高体重、三餐习惯、运动频率、吸烟饮酒、各类食物摄入频次... |

> 光营养问卷一张表就要配 80 多列，每个列名必须和代码完全一致。

#### 4.3 更新 12 组系统配置

```
飞书应用 ID / 密钥              → 2 组
用户表格 Token / Table ID        → 2 组
预约表格 Token / Table ID        → 2 组
档案表格 Token / Table ID        → 2 组
睡眠问卷 Token / Table ID        → 2 组
营养问卷 Token / Table ID        → 2 组
─────────────────────────────
                              共 12 组
```

**任何一组填错，对应数据就同步不过去。**

#### 4.4 历史数据的飞书回写（可选但推荐）

如果需要把已有的 11 条老数据也同步到新飞书表格中，还需要额外做一次批量回写操作。

---

### 第五步：全面联调测试（约 1.5-2 天）

所有东西装好后，需要逐一验证：

| 测试内容 | 涉及范围 |
|---------|---------|
| 老用户登录验证 | 6 个老账号能否用原密码正常登录 |
| 注册 / 登录流程 | 新用户注册是否正常 |
| 预约全流程 | 提交 → 后台看到 → 同步到飞书 |
| 健康档案填写 | 多页长表单 + 图片上传 |
| 睡眠问卷提交 | ~25 个问题项 |
| **营养问卷提交** | **~80+ 个问题项，最复杂** |
| 管理后台全部功能 | 用户管理、预约、档案、Excel 导出 |
| **飞书同步验证** | 每种表单都实际提交一条 → 去飞书确认写入正确 |
| **历史数据完整性** | 逐条核对 6 个用户 + 5 条表单记录是否完好 |
| 异常场景 | 断网、重复提交、特殊字符等 |

---

## 时间估算汇总（已含数据迁移）

| 步骤 | 工作量 | 说明 |
|------|--------|------|
| 新服务器环境搭建 | 0.5 天 | |
| 后端部署 + **数据库迁移** | **1-1.5 天** | 含 5 张表建表 + 11 条历史数据搬迁 + 逐条校验 |
| 前端双系统部署 | 0.5 天 | 用户端 + 管理后台 |
| **飞书配置对接** | **1-1.5 天** | 5 张表共 170+ 字段逐一核对 + 12 组配置更新 |
| 全面联调测试 | 1.5-2 天 | 含历史数据完整性验证 |
| **合计** | **5-6 天** | |

> 对比之前不含数据迁移的版本（4.5-5.5 天），**增加约 0.5-1 天**主要用于数据导出导入和逐条校验。

---

## 需要您提前准备的事项

| 序号 | 事项 | 紧急程度 |
|:----:|------|:-------:|
| 1 | 新服务器的 IP 和登录账号密码 | 必须有 |
| 2 | 新域名（或确认沿用现有域名） | 必须有 |
| 3 | **确定用哪个飞书企业账号** | 必须先确定 |
| 4 | 如用新企业账号，需先把技术人员加为企业成员（或按文档自行操作） | 阻塞项 |
| 5 | **确认历史数据处理方式** | 影响方案 |
| └─ 选项 A | 全部迁移（6 用户 + 5 条表单） | 推荐默认 |
| └─ 选项 B | 只迁移用户账号，表单数据不要了 | 减少工作量 |
| └─ 选项 C | 全部不要，从零开始 | 最简单 |

---

## 特别说明

> 本次迁移**不是简单的复制粘贴**。核心难点在于三套独立系统的联动：
>
> 1. **飞书 170+ 字段必须精确匹配** —— 错一个就导致该类数据同步失败
> 2. **数据库迁移要保证关联关系正确** —— 用户和表单之间有绑定，不能乱了
> 3. **建议安排连续的时间窗口（如假期）**，中途打断容易造成新旧系统状态不一致
