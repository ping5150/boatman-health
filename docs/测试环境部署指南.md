# 船夫健康 — 测试环境部署指南

> ⚠️ 此文档为旧服务器（118.145.239.169）的测试环境记录，当前生产环境已迁移至 106.53.64.33。
> 
> 最后更新：2026-05-03

## 📦 当前生产环境架构

| 组件 | 配置 |
|------|------|
| 服务器 IP | 106.53.64.33 |
| 后端端口 | 3000 |
| 用户端访问 | https://www.boatmanhealth.com/ |
| 管理后台 | https://www.boatmanhealth.com/admin/ |
| 数据库 | SQLite（prod.db） |
| PM2 进程 | boatman-health |
| 后端代码目录 | /www/boatman-health/server/ |
| 前端代码目录 | /www/boatman-health/client/ 和 /www/boatman-health/admin/ |
| Nginx 配置 | /etc/nginx/conf.d/boatman-health.conf |
| SSL 证书 | Let's Encrypt（自动续期） |

---

## 🚀 部署流程

### 部署后端

```bash
# 1. 本地构建
cd server && npm run build

# 2. 上传到服务器
rsync -avz --delete --exclude='node_modules' --exclude='.env' server/ root@106.53.64.33:/www/boatman-health/server/

# 3. 在服务器上安装依赖并重启
ssh root@106.53.64.33 "cd /www/boatman-health/server && npm install --production && npx prisma generate && pm2 restart boatman-health --update-env"
```

### 部署前端

```bash
# 1. 本地构建
cd client && npm run build
cd admin && npm run build

# 2. 上传到服务器
rsync -avz --delete client/dist/ root@106.53.64.33:/www/boatman-health/client/
rsync -avz --delete admin/dist/  root@106.53.64.33:/www/boatman-health/admin/
```

### 首次部署（全新服务器）

详见 PRODUCTION-CONFIG.md 中的完整说明。

---

## ✅ 验证部署

### 1. 检查后端

```bash
# 健康检查
curl https://www.boatmanhealth.com/health

# SSH 到服务器查看
ssh root@106.53.64.33
pm2 list
pm2 logs boatman-health --lines 20
```

### 2. 检查前端

- 用户端：https://www.boatmanhealth.com/
- 管理后台：https://www.boatmanhealth.com/admin/

### 3. 测试 API

```bash
curl https://www.boatmanhealth.com/health
```

---

## 🔧 常用命令

### PM2 管理

```bash
pm2 list                              # 查看状态
pm2 logs boatman-health               # 查看日志
pm2 restart boatman-health --update-env  # 重启服务
pm2 stop boatman-health               # 停止服务
pm2 monit                             # 监控
```

### 数据库管理

```bash
cd /www/boatman-health/server

# 查看数据库
npx prisma studio

# 运行迁移
npx prisma migrate deploy

# 备份数据库
cp prisma/prod.db prisma/prod.db.backup-$(date +%Y%m%d)
```

---

## 🚨 常见问题

### 1. 端口被占用

```bash
lsof -i :3000
kill -9 <PID>
```

### 2. 数据库迁移失败

```bash
cd /www/boatman-health/server
npx prisma migrate deploy
```

### 3. PM2 启动失败

```bash
pm2 logs boatman-health --err
cd /www/boatman-health/server && cat .env
```

### 4. Nginx 配置错误

```bash
nginx -t
tail -f /var/log/nginx/error.log
```

---

## 📞 访问地址汇总

| 环境 | 地址 |
|------|------|
| 生产 - 用户端 | https://www.boatmanhealth.com/ |
| 生产 - 管理后台 | https://www.boatmanhealth.com/admin/ |
| 生产 - API | https://www.boatmanhealth.com/api/ |
| 生产 - 健康检查 | https://www.boatmanhealth.com/health |
